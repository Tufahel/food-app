<%
=begin%>
 <h1> Name: <%= recipe.name %></h1>
<div id="<%= dom_id recipe %>">
     <p>Preparation time: <%= recipe.preperation_time %></p>
     <p>Cooking time: <%= recipe.cooking_time %></p>
</div> 
<%
=end%>

<div>
    <h1 class="text-center" > <%= recipe.name %></h1>
    <div class="d-flex justify-content-around" id="<%= dom_id recipe %>">
     <div>
        <p>Preparation time: <%= recipe.preparation_time %>
         <% if recipe.preparation_time > 1  %>
           hours
           <% else  %>
          hour
         <% end %> </p>
         
        <p>Cooking time: <%= recipe.cooking_time %> 
        <% if recipe.cooking_time > 1  %>
           hours
           <% else  %>
          hour
         <% end %>
         </p>
     </div>
     <div class="form-check form-switch">
        <input class="form-check-input" type="checkbox" id="flexSwitchCheckChecked" checked>
        <label class="form-check-label" for="flexSwitchCheckChecked">Public</label>
     </div>
    </div>
</div>