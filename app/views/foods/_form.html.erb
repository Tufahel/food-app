<%= form_with model: @food, url: foods_path  do |form| %>
  <% if food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(food.errors.count, "error") %> prohibited this food from being saved:</h2>

      <div>
        <%= form.text_field :name, autofocus: true, placeholder: "Enter food name" %>
        <% food.errors.full_messages_for(:name).each do |message| %>
          <div><%= message %></div>
        <% end %>
      </div>
    </div>
    <div>
        <%= form.number_field :measurement_unit, autofocus: true, placeholder: "Enter measurement unit" %>
        <% food.errors.full_messages_for(:measurement_unit).each do |message| %>
          <div><%= message %></div>
        <% end %>
      </div>
    </div>
    <div>
        <%= form.number_field :price, autofocus: true, placeholder: "Enter measurement unit" %>
        <% food.errors.full_messages_for(:price).each do |message| %>
          <div><%= message %></div>
        <% end %>
      </div>
    </div>
  <% end %>
 
  <div>
    <%= form.submit %>
  </div>
<% end %>
